# Railway Environment Variables Template
# Copy this to your Railway project's environment variables

# Database (automatically provided by Railway PostgreSQL service)
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# JWT Configuration (REQUIRED - Generate a secure secret)
JWT_SECRET=your-64-character-jwt-secret-replace-this-with-openssl-rand-hex-32

# Application Configuration
ENVIRONMENT=production
GIN_MODE=release

# Railway automatically sets PORT - do not override unless needed
# PORT=8080

# Rate Limiting (optional - defaults are fine for most cases)
RATE_LIMIT=10.0
RATE_BURST=20

# Database SSL (Railway PostgreSQL requires SSL)
# This is automatically handled when using DATABASE_URL
# DB_SSLMODE=require
